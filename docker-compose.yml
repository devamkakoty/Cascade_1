version: "3.8"

services:
  cascade-predict:
    build: .
    ports:
      - "8501:8501"
    restart: unless-stopped
    environment:
      - STREAMLIT_SERVER_MAX_UPLOAD_SIZE=50
    deploy:
      resources:
        limits:
          memory: 4G
        reservations:
          memory: 1G
